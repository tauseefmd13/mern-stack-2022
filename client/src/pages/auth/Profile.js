import React from "react";
import { Box } from "@mui/material";

const Profile = () => {
	return (
		<Box flex={4} p={{ xs: 0, md: 2 }}>
			<div>Profile</div>
		</Box>
	);
};

export default Profile;
